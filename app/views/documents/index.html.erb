<section class="space-y-5">
  <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
    <div>
      <p class="text-sm font-bold uppercase tracking-normal text-cyan-700">Binder library</p>
      <h1 class="mt-1 text-3xl font-black tracking-normal">Documents</h1>
    </div>
    <% if can_manage_records? %>
      <%= link_to "New document", new_document_path, class: "inline-flex min-h-12 items-center justify-center rounded-md bg-cyan-700 px-5 py-3 text-base font-bold text-white shadow-sm" %>
    <% end %>
  </div>

  <div class="grid gap-3 md:grid-cols-2">
    <% @documents.each do |document| %>
      <div class="rounded-md border border-slate-200 bg-white p-4 shadow-sm">
        <div class="flex items-start justify-between gap-3">
          <div>
            <p class="text-lg font-black"><%= link_to document.title, document_path(document), class: "transition hover:text-cyan-700" %></p>
            <p class="mt-1 text-sm text-slate-600"><%= document.asset&.name || document.account.name %></p>
            <p class="mt-1 text-xs font-semibold text-slate-500">Uploaded <%= local_datetime(document.created_at, account: document.account) %></p>
          </div>
          <div class="flex flex-col items-end gap-2">
            <%= status_chip(document.document_type.humanize, tone: :neutral) %>
            <%= status_chip(document.file.attached? ? "File attached" : "No file attached", tone: document.file.attached? ? :success : :warning) %>
          </div>
        </div>
        <% if document.notes.present? %>
          <p class="mt-3 text-sm leading-6 text-slate-600"><%= document.notes %></p>
        <% end %>
        <div class="mt-4 flex flex-wrap gap-2">
          <%= link_to "View", document_path(document), class: secondary_button_class("min-h-11") %>
          <% if can_manage_account?(document.account) %>
            <%= link_to document.file.attached? ? "Edit" : "Edit / Add file", edit_document_path(document), class: secondary_button_class("min-h-11") %>
          <% end %>
          <% if document.file.attached? %>
            <%= link_to "Open file", url_for(document.file), class: "inline-flex min-h-11 items-center rounded-md bg-slate-900 px-4 py-2 text-sm font-bold text-white" %>
            <%= link_to "Download", rails_blob_path(document.file, disposition: "attachment"), class: "inline-flex min-h-11 items-center rounded-md border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700" %>
          <% end %>
          <% if can_manage_account?(document.account) %>
            <%= button_to "Delete", document_path(document), method: :delete, form: { data: { turbo_confirm: "Delete #{document.title}?" } }, class: "min-h-11 rounded-md border border-red-200 px-4 py-2 text-sm font-bold text-red-700", data: { turbo_submits_with: "Deleting..." } %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</section>
